Unfortunately there are things I would've liked to configure but cannot:

- set default and alternative search engines to just DuckDuckGo
- customize toolbar

so these would need to be done manually for new installs.

---

`2656FF1E876E9973` and `31210A081F86E80E` are CityHashes for the macOS installation directories of Firefox and Firefox Nightly, respectively. They are explicitly used by Firefox to determine what profile to launch with.
